# Example environment variables for LocalTalent
# Copy this to a local `.env` file and edit values for your environment

# Postgres connection
PGUSER=postgres
PGPASSWORD=postgres
PGHOST=localhost
PGDATABASE=local_talent
PGPORT=5432

# Server port (optional override)
PORT=5000
